{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "Thing.schema.json",
  "title": "Thing schema",
  "allOf": [
    {
      "$ref": "./Config.schema.json"
    },
    {
      "type": "object",
      "properties": {
        "interactionGoal": {
          "type": "integer",
          "default": 0,
          "minimum": 0,
          "description": "Objetivo de interacciones para esta configuración."
        },
        "interactionGoalReward": {
          "type": "string",
          "default": "0",
          "description": "Lista elementos que desbloquea el interaction goal (items, rooms, actors, props)."
        },
        "killGoal": {
          "type": "integer",
          "default": 0,
          "minimum": 0,
          "description": "Objetivo de muertes para esta configuración."
        },
        "killGoalReward": {
          "type": "string",
          "default": "0",
          "description": "Lista elementos que desbloquea el kill goal (items, rooms, actors, props)."
        },
        "maxPerRoom": {
          "type": "integer",
          "default": 0,
          "description": "Número maximo de veces que este elemento puede aparecer por 'room'.",
          "minimum": 0
        },
        "maxSpawnAmount": {
          "type": "integer",
          "default": 1,
          "minimum": 1,
          "description": "Número maximo de veces que este elemento se spawnea en el 'room'."
        },
        "minSpawnAmount": {
          "type": "integer",
          "default": 1,
          "minimum": 1,
          "description": "Número mínimo de veces que este elemento se spawnea en el 'room'."
        },
        "sideRomOnly": {
          "type": "boolean",
          "description": "Solo puede spawnear en side rooms."
        },
        "spawnChance": {
          "type": "number",
          "default": 1,
          "minimum": 0,
          "maximum": 1,
          "description": "Chances de spawn."
        },
        "ticketRewardAmount": {
          "type": "integer",
          "default": 0,
          "minimum": 0,
          "description": "Máximo de tickets que puede dropear."
        },
        "ticketRewardChance": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "maximum": 1,
          "description": "Chances de dropear tickets."
        },
        "usePlaceholder": {
          "type": "boolean",
          "description": "Indica si tiene que ser spawneado en un placeholder del room."
        }
      }
    }
  ]
}