{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "Thing.schema.json",
  "title": "Thing schema",
  "allOf": [
    {
      "$ref": "./Entity.schema.json"
    },
    {
      "type": "object",
      "properties": {
        "requiresDeadEnd": {
          "type": "boolean",
          "description": "Solo puede spawnear en rooms que son callejones sin salida."
        },
        "maxPerRoom": {
          "type": "integer",
          "default": -1,
          "description": "Número maximo de veces que este elemento puede aparecer por 'room'."
        },
        "maxSpawnAmount": {
          "type": "integer",
          "default": 1,
          "minimum": 1,
          "description": "Número maximo de veces que este elemento se spawnea en el 'room'."
        },
        "minSpawnAmount": {
          "type": "integer",
          "default": 1,
          "minimum": 1,
          "description": "Número mínimo de veces que este elemento se spawnea en el 'room'."
        },
        "placements": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "Floor",
              "WallFrontBase",
              "WallFrontHang",
              "WallLeftBase",
              "WallLeftHang",
              "WallRightBase",
              "WallRightHang",
              "Ceiling",
              "WalkArea"
            ]
          },
          "default": [],
          "description": "Lista de superficies donde puede spawnear. Si está vacío, se spawnea en el WalkArea."
        }
      }
    }
  ]
}