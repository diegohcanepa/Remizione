{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "Prop.schema.json",
  "title": "Prop Data File",
  "type": "object",
  "properties": {
    "data": {
      "type": "array",
      "description": "Lista de definiciones de Props.",
      "items": {
        "$ref": "#/$defs/Prop"
      }
    }
  },
  "required": [
    "data"
  ],
  "$defs": {
    "Prop": {
      "description": "Un Prop hereda todo de Thing (y Core) pero se define aquí para validación específica.",
      "allOf": [
        {
          "$ref": "./Thing.schema.json"
        }
      ]
    }
  }
}