{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "Config.schema.json",
  "title": "Config base",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Nombre único de la configuración. (Requerido)"
    },
    "weight": {
      "type": "number",
      "format": "float",
      "default": 1.0,
      "minimum": 0,
      "description": "Peso o probabilidad asociada a esta configuración. Por defecto es 1."
    },
    "maxPerRun": {
      "type": "integer",
      "minimum": 0,
      "description": "Número máximo de veces que este elemento puede aparecer por 'run'."
    },
    "requiredCompletedRuns": {
      "type": "integer",
      "minimum": 0,
      "description": "Número de 'runs' completadas requeridas para que esta configuración sea elegible."
    },
    "requiredRuns": {
      "type": "integer",
      "minimum": 0,
      "description": "Número total de 'runs' jugadas requeridas para que esta configuración sea elegible."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Lista de etiquetas (strings) usadas para aplicar restricciones de 'scope' (Tags)."
    },
    "pools": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Lista de 'pools' a las que pertenece esta configuración (Tags)."
    }
  },
  "required": [ "name" ]
}
