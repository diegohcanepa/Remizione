namespace Remizione
{
    /// <summary>
    /// BloodyEye
    /// </summary>
    public sealed class BloodyEye : Actor
    {
        // Constructor
        public BloodyEye(GameSession session, string name)
            : base(session, name)
        {
        }
    }
}
