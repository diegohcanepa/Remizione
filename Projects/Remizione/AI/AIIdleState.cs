namespace Remizione
{
    /// <summary>
    /// AIIdleState
    /// </summary>
    public sealed class AIIdleState : AIState
    {
        // Constructor
        public AIIdleState(AIStateMachine stateMachine)
            : base(stateMachine)
        {
        }
    }
}
